export async function sendSms({ to, message }) {
  // Stub for MSG91/Twilio: replace implementation here only.
  // eslint-disable-next-line no-console
  console.log(`[SMS STUB] to=${to} message="${message}"`)
  return { ok: true }
}

